# Smart Import - Умный импорт с Column Mapping

## Обзор

Новая функция **Smart Import** позволяет импортировать клиентов из CSV файлов любого формата с помощью визуального сопоставления колонок (column mapping).

## Доступ

- **URL:** `/AR/Clients/SmartImport`
- **Меню:** Clients → Import (dropdown) → Smart Import (новый!)

## Возможности

### ✨ Автоматическое определение формата
- **QIMy:** Внутренний формат QIMy
- **BMD NTCS:** Экспорт из BMD бухгалтерии
- **Пользовательский:** Любой CSV формат с ручным mapping

### 🎯 Визуальное сопоставление колонок
- Drag & Drop интерфейс для сопоставления полей
- Предпросмотр примеров данных из каждой колонки
- Автоматическое сопоставление по названиям (Auto-map)
- Индикация обязательных полей (красная рамка)

### 👁️ Интерактивный предпросмотр
- Просмотр данных перед импортом
- Статистика: всего/валидных/с ошибками
- Детальная информация об ошибках
- Возможность исправить mapping и перепросмотреть

### 🛡️ Валидация
- Проверка обязательных полей (ClientCode, CompanyName)
- Проверка формата данных (числа, даты, и т.д.)
- Пропуск или остановка при ошибках

## Пошаговая инструкция

### Шаг 1: Выбор файла
1. Откройте `/AR/Clients/SmartImport`
2. Нажмите "Выбрать файл" и выберите CSV файл
3. Нажмите **"Анализировать файл"**

### Шаг 2: Column Mapping
1. **Слева:** Видите колонки из вашего CSV файла
2. **Справа:** Поля QIMy (обязательные отмечены красным)
3. **Сопоставление:**
   - **Вариант A:** Нажмите **"Автоматическое сопоставление"** (рекомендуется)
   - **Вариант B:** Перетащите колонку слева в поле справа
4. Проверьте статистику: "Обязательных полей не заполнено: 0"
5. Нажмите **"Предпросмотр с этим mapping"**

### Шаг 3: Предпросмотр
1. Просмотрите таблицу с данными
2. Проверьте статистику валидации
3. Если есть ошибки:
   - **"Изменить mapping"** → вернуться к Шагу 2
   - **"Пропустить ошибки"** → импортировать только валидные
4. Нажмите **"Выполнить импорт"**

### Шаг 4: Результат
- Импортировано: X клиентов ✅
- Ошибок: Y строк ❌
- Пропущено: Z дубликатов ⚠️
- Детальный лог ошибок (если есть)

## Поддерживаемые поля

### Обязательные
- `ClientCode` - Код клиента (числовой)
- `CompanyName` - Название компании

### Опциональные
- `Country` / `CountryCode` - Страна / Код страны
- `Address` - Адрес
- `PostalCode` - Почтовый индекс
- `City` - Город
- `VatNumber` - UID-номер (VAT Number)
- `Email` - Email
- `Phone` - Телефон
- `ContactPerson` - Контактное лицо
- `AccountNumber` - Номер счета
- `PaymentTerms` - Условия оплаты
- `DiscountPercent` - Процент скидки
- `DiscountDays` - Дни скидки
- `Currency` - Валюта
- `TaxNumber` - Налоговый номер
- `Branch` - Филиал
- `Description` - Описание

## Примеры Column Mapping

### BMD NTCS → QIMy
```
CSV Колонка       →  QIMy Поле
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Kto-Nr            →  ClientCode
Nachname          →  CompanyName
Straße            →  Address
Plz               →  PostalCode
Ort               →  City
UID-Nummer        →  VatNumber
WAE               →  Currency
ZZiel             →  PaymentTerms
Land-Nr           →  CountryCode
```

### Excel Export → QIMy
```
CSV Колонка            →  QIMy Поле
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Customer ID            →  ClientCode
Company Name           →  CompanyName
Street Address         →  Address
ZIP Code               →  PostalCode
City                   →  City
VAT ID                 →  VatNumber
Email Address          →  Email
```

## Технические особенности

### Кодировка
- **Windows-1252** (автоматически)
- Поддержка немецких символов: ä, ö, ü, ß
- Поддержка русских символов: а-я, А-Я

### Разделитель
- **Точка с запятой (;)** - основной
- Автоматическое определение

### Размер файла
- Максимум: **10 MB**
- Рекомендуется: до 10,000 строк для быстрого предпросмотра

### Дубликаты
- Проверка по `ClientCode`
- Существующие клиенты **пропускаются** (не обновляются)

## Сравнение с Standard Import

| Возможность | Standard Import | Smart Import |
|------------|-----------------|--------------|
| Фиксированный формат | ✅ QIMy только | ❌ Любой формат |
| BMD поддержка | ✅ Авто-определение | ✅ С column mapping |
| Пользовательский CSV | ❌ Нет | ✅ Есть |
| Визуальный mapping | ❌ Нет | ✅ Drag & Drop |
| Auto-mapping | ❌ Нет | ✅ По названиям |
| Предпросмотр примеров | ❌ Только данные | ✅ Примеры из каждой колонки |
| Скорость | ⚡ Быстрее | 🐢 Медленнее |

## Советы и лучшие практики

### 1. Используйте Auto-mapping
- Сэкономит 90% времени
- Работает, если названия колонок похожи на стандартные

### 2. Проверяйте предпросмотр
- Всегда смотрите примеры данных перед импортом
- Убедитесь, что данные в правильных полях

### 3. Начните с малого
- Первый раз импортируйте 5-10 строк для теста
- Проверьте результат
- Затем импортируйте весь файл

### 4. Подготовьте CSV
- Удалите лишние пустые строки
- Убедитесь, что первая строка - это заголовок
- Проверьте кодировку (Windows-1252)

### 5. Обязательные поля
- `ClientCode` должен быть **числовым**
- `CompanyName` не должно быть пустым
- Без этих полей строка будет пропущена

## Устранение проблем

### "Обязательных полей не заполнено: 2"
**Причина:** Не сопоставлены ClientCode и/или CompanyName  
**Решение:** Проверьте column mapping, убедитесь, что эти поля связаны

### "ClientCode must be a valid number"
**Причина:** В колонке ClientCode не числовые значения  
**Решение:** Проверьте CSV, удалите нечисловые символы или выберите другую колонку

### "Showing first 100 rows of 5000 total rows"
**Причина:** Предпросмотр ограничен 100 строками  
**Решение:** Это нормально, все 5000 строк будут импортированы при Execute

### Файл не анализируется
**Причина:** Неправильная кодировка или формат  
**Решение:** Убедитесь, что файл в формате CSV (не XLS/XLSX), пересохраните с кодировкой Windows-1252

## Известные ограничения

1. **Максимум 10 MB** - большие файлы разбивайте на части
2. **Только импорт** - нет обновления существующих клиентов
3. **Только CSV** - Excel файлы нужно экспортировать в CSV
4. **Drag & Drop** - работает только мышью (нет клавиатуры)

## Roadmap (будущие улучшения)

- [ ] Сохранение mapping шаблонов ("Save mapping profile")
- [ ] Импорт из Excel напрямую (.xlsx)
- [ ] Batch update (обновление существующих клиентов)
- [ ] Импорт других сущностей (Suppliers, Products, Invoices)
- [ ] Экспорт в любой формат с custom mapping
- [ ] API endpoint для автоматического импорта

## Техническая документация

### Компоненты
- **Page:** `SmartImport.razor`
- **Component:** `CsvColumnMapper.razor`
- **Service:** `ClientImportService.AnalyzeCsvStructureAsync()`
- **Service:** `ClientImportService.PreviewImportAsync(columnMapping)`

### Workflow
```
1. Upload CSV → 2. Analyze Structure → 3. Column Mapping → 4. Preview → 5. Import
   ↓                  ↓                    ↓                   ↓            ↓
IBrowserFile    Headers + Samples    Dictionary<string,   List<DTO>   Database
                                      string>
```

### API
```csharp
// Analyze CSV structure
(List<string> Headers, Dictionary<string, string> SampleData) 
  AnalyzeCsvStructureAsync(Stream fileStream)

// Preview with custom mapping
List<ClientImportDto> 
  PreviewImportAsync(Stream fileStream, Dictionary<string, string>? columnMapping)

// Execute import
ImportResult 
  ImportClientsAsync(Stream fileStream, bool skipErrors)
```

## Поддержка

- **Документация:** Этот файл
- **Логи:** `C:\Projects\QIMy\logs\`
- **GitHub Issues:** [создать issue]

---

**Последнее обновление:** 25 января 2026  
**Версия:** 1.0.0  
**Автор:** QIMy Development Team
